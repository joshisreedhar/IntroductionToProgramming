n=int(input())
i=0
space=2*n-2
while i<n:
	for j in range(0,space):
		print(end=" ")
	space=space-1
	num=1
	for j in range(0,i+1):
		 
		 print(num,end="")
		 num=num+1
	alpha=64+i
		 
	for j in range (0,i):
		ch=chr(alpha)
		print(ch,end="")
		alpha=alpha-1
	i=i+1
	print("\r")
